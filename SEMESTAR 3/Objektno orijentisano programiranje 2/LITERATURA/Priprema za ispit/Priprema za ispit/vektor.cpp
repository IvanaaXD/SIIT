#include <vector>
#include <iostream>

using namespace std;

/*std::vector<int> operator+(const std::vector<int>& x, const std::vector<int>& y) {
    std::vector<int> novi;

    // Copy elements from vector x
    for (std::vector<int>::const_iterator it = x.begin(); it != x.end(); ++it) {
        novi.push_back(*it);
    }

    // Copy elements from vector y
    for (std::vector<int>::const_iterator it = y.begin(); it != y.end(); ++it) {
        novi.push_back(*it);
    }

    return novi;
}
*/
/*int main()
{
    std::vector<int> x = { 1, 2 };
    std::vector<int> y = { 3, 4 };
    std::vector<int> z = x + y;

    for (std::vector<int>::iterator it = z.begin(); it != z.end(); ++it) {
        cout << *it << " ";
    }

    // Expected output: 1 2 3 4

    return 0;
}
*/